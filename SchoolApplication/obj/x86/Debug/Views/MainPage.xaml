<Page x:ConnectionId='1'
    x:Class="SchoolApplication.Views.MainPage"
    xmlns:local="using:SchoolApplication.Views"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Style="{StaticResource PageStyle}"
    mc:Ignorable="d">
    <Page.Resources>
        <CollectionViewSource x:ConnectionId='2' x:Name="StudentCollection"                                         d:Source="{Binding ViewModel}" />
        <CollectionViewSource x:ConnectionId='3' x:Name="CourseCollection"                                       />
    </Page.Resources>

    <Grid x:ConnectionId='4'
        x:Name="ContentArea"
        Margin="{StaticResource MediumLeftRightMargin}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>


        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Students" FontSize="24"  Grid.Row="0"/>
            <ListView x:ConnectionId='7' Name="Students" ItemsSource="{Binding Source={StaticResource StudentCollection}}" Grid.Column="0" Grid.Row="1">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding FullName}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Courses" FontSize="24"  Grid.Row="0"/>
            <ListView x:ConnectionId='5' Name="Courses"  ItemsSource="{Binding Source={StaticResource CourseCollection}}" Grid.Column="0" Grid.Row="1">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding CourseName}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

    </Grid>
</Page>

